$HOME $ cd data
$HOME/data $ ls | grep texte
texte
$HOME/data $ cd folder1
$HOME/data/folder1 $ ls | grep folder1
folder1
$HOME/data/folder1 $ ls | grep folder2
$HOME/data/folder1 $ ls | grep birds
three-little-birds
$HOME/data/folder1 $ ls | grep secret
$HOME/data/folder1 $ exit
